{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "bash -lc 'source .venv/bin/activate && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000'"
  },
  "dependencies": {
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0"
  }
}